class Main inherits IO {
  main() : Object { 
    {
        let type : Int <- (new Int) in
        let test : String <- test in
        let spog : Test in
        nonexistent_method(blah);
    }
  } ;
} ; 
